# 自托管推荐：走同源 `/api`（由前端 Nginx 反代到后端），避免 CORS 与硬编码域名
VITE_API_BASE_URL=/api
# Railway 单独部署前端镜像时，在服务的 Build Variables 注入后端公网 API（示例）：
# VITE_API_BASE_URL=https://<your-backend>.up.railway.app/api
VITE_APP_NAME=AI 测试用例平台
