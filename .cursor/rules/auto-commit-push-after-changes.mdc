---
description: 会话内代码修改完成后自动提交并推送到 main，以触发 Railway 等部署
alwaysApply: true
---

# 修改后自动提交与推送（含「合并」到主分支）

## 适用范围

在**本会话**中完成了对仓库内**源代码或配置**的修改（不含仅聊天、仅查文档）后，在任务收尾**默认执行**提交与推送，使用户无需再手动 `git push`。

## 「合并」在本仓库中的含义

默认分支为 **`main`**。将提交推送到 **`origin/main`** 即视为变更已合并进主开发线并触发绑定该分支的部署（如 Railway）。**不**额外创建 PR，除非用户明确要求走 Pull Request 流程。

## 执行步骤（收尾必做）

1. **`git status`**：确认变更文件；勿提交 `.gitignore` 中的产物（如 `frontend/dist.zip`），除非用户明确要求提交产物。
2. **`git add`**：仅纳入与本次任务相关的路径。
3. **`git commit -m "..."`**：使用完整、可读的英文或中文说明（说明「改了什么、为什么」）。
4. **`git push origin main`**（或用户指定的分支）：推送失败时重试或说明原因（权限、网络、冲突需先 `pull` 等）。

## 与 GitHub MCP 规则的关系

若项目规则要求 **优先使用 GitHub MCP**（`push_files` 等），则：

- **优先**按 `github-mcp-deploy.mdc` 用 MCP 发布到同一仓库与分支；
- 当 MCP **不可用**或用户环境更适合终端时，使用 **`git push`**，并在回复中说明采用了何种方式。

## 例外（不自动推送）

- 用户明确说 **不要提交 / 不要推送 / 仅本地保留**。
- 变更 **明显未完成**、编译失败或测试未通过且用户未接受「先推再修」。
- **纯只读**会话（无文件修改）。

## 前端构建产物

若本次改动了 `frontend/` 且规则要求打包：先 **`pnpm build`** 与 **`dist.zip`**，再提交/推送；`dist.zip` 若在 `.gitignore` 中则**不**纳入 commit，仅在回复中告知本地路径即可。
