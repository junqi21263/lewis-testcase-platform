# 分支工作流规则（开发与生产隔离）

## 核心规则

**所有前后端开发必须遵循以下流程：**

1. **开发阶段**：
   - 所有新功能、bug修复、优化都**必须在 `develop` 分支**进行
   - 禁止直接在 `main` 分支开发

2. **推送 `develop` 后的行为**：
   - 当检测到用户在 `develop` 分支推送代码（`git push origin develop`）后，**必须主动询问**：
     - 是否要创建 PR 合并到 `main`？
     - 是否需要先进行代码审查？
     - 是否准备好部署到生产环境？

3. **部署前必须确认**：
   - **每次**执行部署相关操作（包括但不限于 `git push origin main`、手动部署、触发 CI/CD）前，**必须询问用户确认**：
     - “是否确认将 develop 分支的变更合并/部署到生产环境（main）？”
     - 只有在用户明确回复“是”或“确认”后，才能继续执行合并或部署操作。

4. **合并流程**：
   - 推荐通过 GitHub Pull Request 从 `develop` → `main`
   - 合并前应进行代码审查
   - 合并到 `main` 后会触发生产环境的自动部署

## 适用范围

- 前端（`frontend/`）
- 后端（`backend/`）
- 配置文件、文档、CI/CD 工作流
- 所有与业务代码、部署相关的变更

## 例外情况

仅当用户**明确指示**“直接在 main 分支工作”或“跳过确认直接部署”时可以例外，但仍需在响应中记录此决定。

## 提示词要求

在每次对话中：
- 如果用户在 `develop` 分支工作并准备推送，主动提醒分支策略
- 每次涉及部署的操作前，必须询问确认
- 保持友好、专业，解释这样做的目的是保证生产环境稳定性

此规则优先级高于其他通用规则。
