---
description: 开发/生产环境发布守门规则：每次合并/部署都要询问确认
alwaysApply: true
---

# 开发/生产环境发布守门（必须询问确认）

## 目标

确保代码变更始终先在 **`develop`** 验证，通过后再进入 **`main`**（生产），并且每次发布都由用户明确确认。

## 强制流程（必须遵守）

- **开发只在 `develop`**：
  - 任何功能开发、调试、验证、CI 试跑，都在 `develop` 分支进行。
  - 默认只允许把提交推送到 `origin/develop`。

- **生产只在 `main`**：
  - 只有当用户明确说“要上线 / 合并到 main / 部署生产”时，才允许对 `main` 做任何操作（merge / push / release）。

## 每次部署都必须询问

出现以下任意动作时，**必须先询问用户是否执行**，并等待明确同意（例如“是”“可以”“合并到 main”“部署生产”）：

- 推送到会触发部署的分支（例如 `main`，以及任何已配置为自动部署的分支）
- 合并到 `main`
- 触发/重跑生产部署工作流
- 在生产服务器上执行 `docker compose up -d --build` 或等价命令

## 推荐提示话术

在你准备进行发布动作前，先问：

- “我将把当前变更 **合并到 `main` 并触发生产部署**，是否现在执行？”
- “我将推送到 **`develop` 并触发开发环境部署**，是否现在执行？”

