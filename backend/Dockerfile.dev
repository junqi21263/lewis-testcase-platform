FROM node:20

WORKDIR /app

RUN apt-get update && apt-get install -y \
    openssl \
    python3 \
    make \
    g++ \
    pkg-config \
    libcairo2-dev \
    libjpeg-dev \
    libpango1.0-dev \
    libgif-dev \
    librsvg2-dev \
    && rm -rf /var/lib/apt/lists/*

RUN corepack enable

COPY backend/package.json backend/pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile --ignore-scripts

COPY backend/ ./
RUN pnpm prisma generate --schema=./prisma/schema.prod.prisma
RUN pnpm rebuild canvas && node -e "const p=require.resolve('canvas'); require(p); console.log('canvas native OK:', p)"

EXPOSE 3000

CMD ["pnpm", "start:dev"]